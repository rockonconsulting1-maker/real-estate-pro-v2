Milestone 1 (Supabase side) is deployed, live, and verified against your real GHL dev sub-account. Here's where each task stands.
SB-1.1 ghl-proxy — done, live at v2. JWT-verified pass-through at {SUPABASE_URL}/functions/v1/ghl-proxy/<ghl-path>. Verified live: GET /locations/{id}, /opportunities/pipelines (Buyer Transaction pipeline came back with all 10 stages), /contacts/ list, and /objects/ schemas all returned real data from "Real Estate Pro CRM - Dev". No JWT → 401; anon-key-only → normalized 401; /payments → 403 forbidden_path; a leakage scan across responses found no PIT anywhere. Uses your secrets: GHL_BASE_URL, GHL_API_VERSION (Version: v3 sent on every call, per-request override via x-ghl-version for anything still expecting 2021-07-28 during migration), and GHL_PIT/GHL_LOCATION_ID as the dev fallback when a user has no ghl_credentials row.
One thing worth knowing: my first burst test proved in-memory rate limiting doesn't hold across Edge Function isolates, so "central" would have been a lie. I replaced it with a Postgres-backed gate — ghl_proxy_gate() enforces a 90-req/10s window per user and returns the PIT in the same call, so it costs zero extra roundtrips versus the plain credentials select. Verified: 95 calls → 90 allowed, 5 blocked with retryAfter, and client roles can't execute the function. GHL-side 429s are retried in the proxy honoring Retry-After.
SB-1.2 — done. pit_token is now write-only from the client: column-level SELECT revoked from anon/authenticated (verified — select pit_token returns permission denied while the other columns read fine), owner insert/update/delete intact, proxy still resolves the token via service role. Heads-up for the frontend: select('*') on ghl_credentials will now fail — explicit column lists only.
SB-1.3 — done, live at v2. POST /ghl-credentials validates against GET /locations/{id} before saving; POST /ghl-credentials/test exercised the full path live (ok: true, location name resolved, credentialSource: "env"). Distinguishable errors verified: bogus PIT → invalid_token with ghlStatus: 401; missing fields → validation.
SB-1.4 — wired, needs one secret. Both functions init Sentry with function/environment/region tags and a beforeSend scrubber that strips headers and bodies so the PIT can never leak into an event. It currently no-ops because there's no SENTRY_DSN in your secrets (I tried pulling a DSN via your Sentry connector but the call wasn't approved). To finish the acceptance test: add SENTRY_DSN (backend project DSN) and optionally ENVIRONMENT to the project secrets, then force an error (e.g. hit the proxy with a valid session and a malformed GHL call) and confirm it lands tagged source=edge-function.
